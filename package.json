{
  "name": "runcraft",
  "version": "1.0.0",
  "description": "Out & Back Running Route Planner",
  "scripts": {
    "start": "npx serve public -l ${PORT:-3000} -s"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
